import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_squared_error
from datetime import datetime, timedelta

# Load energy consumption dataset
# Replace 'energy_data.csv' with your dataset path
data = pd.read_csv('energy_data.csv')# Optimization function
def optimize_energy_usage(current_hour, energy_threshold):
    # Predict consumption for the next 24 hours
    future_hours = pd.DataFrame({
        'hour': [(current_hour + i) % 24 for i in range(24)],
        'day_of_week': [datetime.now().weekday()] * 24,
        'month': [datetime.now().month] * 24
    })
    predictions = model.predict(future_hours)

    # Suggest hours for energy-intensive tasks
    optimal_hours = [hour for hour, pred in zip(future_hours['hour'], predictions) if pred < energy_threshold]
    return optimal_hours

# Example usage
current_hour = datetime.now().hour
energy_threshold = 50  # Define your own threshold
optimal_hours = optimize_energy_usage(current_hour, energy_threshold)
